---
meta:
  environment: NAME
  dns_root: microbosh
  security_groups:
    - node-exporter

  stemcell:
    name: STEMCELL

director_uuid: PLACEHOLDER-DIRECTOR-UUID

releases:
  - name: node-exporter
    version: latest
